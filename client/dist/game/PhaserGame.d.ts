import Phaser from 'phaser';
/**
 * Create and return a Phaser.Game instance.
 * Mounts into the given parent DOM element.
 */
export declare function createPhaserGame(parent: HTMLElement): Phaser.Game;
